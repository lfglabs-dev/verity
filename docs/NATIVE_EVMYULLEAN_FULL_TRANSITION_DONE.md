# Full Native EVMYulLean Transition: Definition Of Done

This document records the completed transition from Verity's former Yul
interpreter path to native EVMYulLean execution as the authoritative
compiler-verification target.

The SimpleStorage native theorem discharges its retrieve, store, and selector
bridge cases, and the public generated-dispatcher theorem family targets native
EVMYulLean dispatcher execution. Removed transition evidence used to record a
Verity-side Yul statement-interpreter path through the EVMYulLean-backed builtin
backend.

The removed backend-fuel theorem family marks the old shape that has now been
deleted. The public compiler-correctness theorem targets native EVMYulLean
execution directly through `EvmYul.Yul.callDispatcher`; that condition is
satisfied on `main`.

## Current Baseline

The current baseline is:

- Layer 2 proves semantic preservation from supported `CompilationModel`
  programs to IR.
- Public EndToEnd proofs now target
  `Compiler.Proofs.YulGeneration.Backends.Native.interpretIRRuntimeNative`,
  which executes through native EVMYulLean. Helper-free and mapping-helper
  generated-dispatcher wrappers consume concrete dispatcher lowering and build
  full emitted-runtime native lowering internally.
- Lower Layer 3 transition modules no longer contain the old builtin
  comparison path or the backend-parameterized builtin wrapper.
- `simpleStorage_endToEnd_native_evmYulLean` is a concrete native theorem over
  the direct projected `EvmYul.Yul.callDispatcher` result, with no public
  retrieve-hit, store-hit, or selector-miss bridge premises.
- The active compiler stack has zero project-level Lean axioms and zero `sorry`
  placeholders.

## Final Architecture

The completed proof path must be:

```text
CompilationModel
  -> IRContract
  -> emitted Verity Yul runtime
  -> EVMYulLean YulContract
  -> EvmYul.Yul.callDispatcher
  -> projected observable result
  -> public end-to-end theorem
```

Differential tests may still compare generated code against external execution
engines, but the public semantic-preservation proof path must not depend on an
alternate Verity Yul interpreter.

## Non-Negotiable Completion Criteria

The transition is done only when all criteria in this section are true on
`main`.

### 1. Public Theorem Target

- The generic Layer 3 and end-to-end compiler-correctness theorem statements
  target native EVMYulLean execution, not the removed backend-parameterized
  interpreter path.
- The theorem-facing native target is either `Native.interpretIRRuntimeNative`
  or a narrower wrapper that internally lowers to EVMYulLean and executes via
  `EvmYul.Yul.callDispatcher`.
- No public theorem keeps a native-vs-alternate-interpreter agreement premise such
  as `nativeIRRuntimeAgreesWithEvmYulLeanFuelWrapper`,
  `nativeCallDispatcherAgreesWithEvmYulLeanFuelWrapper`, or an equivalent bridge
  assumption.
- Any remaining comparison with non-proof execution engines is explicitly
  marked as differential testing, not as the authoritative proof target.

### 2. Generated Fragment Characterization

- The compiler-emitted Yul fragment is represented by named syntactic
  predicates or theorem hypotheses that can be generated from the compiler
  output without manual contract-specific proof work.
- The characterization covers generated statements, expressions, helper
  functions, dispatcher shape, storage layout, memory/return ABI snippets,
  event/log snippets, selector extraction, callvalue guards, calldata guards,
  and native switch temporaries.
- Unsupported constructs fail closed before the public theorem claims semantic
  preservation. They must not silently fall back to an alternate execution
  semantics.
- The generated-fragment contract is tested by CI so new emitted Yul forms
  cannot bypass the native proof surface.

### 3. Native State Bridge

- Every transaction, environment, and state field reachable from generated Yul
  has one of three statuses:
  - bridged with a theorem,
  - explicitly constrained by a theorem-facing invariant, or
  - rejected fail-closed before execution.
- The bridge covers selector/calldata bytes, calldata word reads, caller,
  source, current address, callvalue, timestamp, block number, supported chain
  and blob fields, storage, transient storage if emitted, memory, returndata,
  logs, and rollback behavior.
- State projection uses bounded EVM-compatible storage keys and words. No proof
  relies on Nat slot injectivity after coercion to 256-bit EVM keys.
- Any EVMYulLean fork divergence required by the state bridge is either
  upstreamed or documented in the fork audit with a non-semantic explanation.

### 4. Native Statement And Expression Semantics

- For every statement and expression form generated by the compiler, there is a
  theorem proving native EVMYulLean execution agrees with the source/IR
  semantics used by the compiler proof.
- Coverage includes literals, locals, assignments, blocks, conditionals,
  switches, loops if emitted, function/helper calls, arithmetic, comparisons,
  bit operations, calldata operations, storage operations, memory operations,
  mapping-slot helpers, returns, stops, reverts, and logs.
- Dispatcher execution is proved for selector hits, selector misses, default
  arms, duplicate-case fail-closed behavior, matched-flag preservation, and
  generated native switch temporary freshness.
- External calls, static calls, returndata, and gas-sensitive behavior are
  either fully modeled and proved or syntactically excluded from the generated
  native fragment used by the public theorem.

### 5. Result Projection

- Native success, stop, return, revert, and hard-error results are projected to
  the theorem-facing observable result with proved lemmas.
- Projection covers success flags, return values, final storage on observable
  slots, final mappings if still present in the result shape, event/log
  histories, rollback semantics, and missing-account/missing-slot defaults.
- Non-word-sized return or revert buffers are either modeled faithfully or
  excluded by a generated-fragment theorem.
- Projection lemmas are not contract-specific; concrete contract theorems may
  instantiate them, but the generic transition cannot depend on SimpleStorage
  closed forms.

### 6. Fuel And Termination Story

- Fuel cannot appear as an unexplained public assumption.
- The public theorem uses one of these designs:
  - a fuel-parametric theorem with explicit monotonicity/sufficiency facts,
  - a generated-code fuel lower bound proved from the emitted runtime, or
  - a total wrapper that chooses and proves a sufficient fuel internally.
- Fuel exhaustion must project as a closed failure only when the theorem
  explicitly reasons about that failure. It must not be confused with semantic
  disagreement.

### 7. Proof Cleanliness

- No `sorry`, `admit`, `axiom`, or theorem declaration marked as an assumption
  is introduced for the transition.
- `AXIOMS.md`, generated axiom reports, and `PrintAxioms.lean` agree that the
  active compiler stack has zero project-level Lean axioms.
- The public native theorem's `#print axioms` output contains only Lean
  foundational axioms such as `propext`, `Classical.choice`,
  `Lean.ofReduceBool`, or `Quot.sound`.
- There are no hidden local assumptions that reintroduce alternate interpreter
  correctness, Nat storage-slot injectivity, unbounded storage values, or
  unconstrained environment defaults.

### 8. Documentation And Trust Boundary

- `README.md`, `AUDIT.md`, `TRUST_ASSUMPTIONS.md`, and
  `docs/VERIFICATION_STATUS.md` describe native EVMYulLean as the
  authoritative semantic target.
- Removed alternate-interpreter paths do not appear in the checked-in proof tree
  or theorem-facing reports.
- EVMYulLean fork audit artifacts and capability reports are regenerated and
  checked.
- The transition docs name all remaining excluded features and link them to
  issue-tracked scope decisions.

### 9. CI Enforcement

- CI fails if the public compiler theorem drifts away from the native
  EVMYulLean dispatcher path.
- CI checks the generated-fragment boundary, native transition docs, bridge
  coverage reports, fork audit artifacts, axiom report, proof-length metadata,
  and native smoke/regression tests.
- The following local validations pass before merge:

```bash
lake build Compiler.Proofs.YulGeneration.Backends.EvmYulLeanNativeHarness
lake build Compiler.Proofs.YulGeneration.Backends.EvmYulLeanNativeLowering
lake build Compiler.Proofs.EndToEnd
lake build Compiler
lake build Contracts
lake build PrintAxioms
python3 scripts/generate_print_axioms.py --check
python3 scripts/check_native_transition_doc.py
python3 scripts/check_bridge_coverage_sync.py
python3 scripts/check_evmyullean_capability_boundary.py
make check
```

### 10. Merge Readiness

- Every PR in the transition stack is based on `main` or the PR directly below
  it.
- Required-equivalent GitHub checks are green on the head commit.
- Cursor Bugbot, Codex, or maintainer review threads on the head commit are
  resolved.
- Generated files are produced by scripts, not hand-edited.
- Branches are pushed with `git push --force-with-lease` when rebasing is
  necessary; plain `--force` is never used.

## Incremental PR Shape

Each PR should move exactly one durable boundary:

1. Native generated-fragment predicate or checker.
2. Native state/environment bridge theorem family.
3. Native result projection theorem family.
4. Native statement/expression equivalence for one generated construct family.
5. Dispatcher/native-switch preservation.
6. Fuel sufficiency or fuel-parametric theorem layer.
7. Fuel adequacy and native execution modularity for generated Yul.
8. Public theorem target maintenance.
9. CI/doc/trust-boundary cleanup after proof-boundary changes.
10. Keep removed interpreter/adapter surfaces out of the public proof path.

Do not mix proof work with unrelated cleanup. Do not reintroduce alternate
interpreter paths as proof authority.

## Reusable Agent Prompt

Use this prompt for repeated focused sessions on the transition. Set
`VERITY_REPO` to the checkout path and `VERITY_MEMORY_DIR` to the local memory
directory before using it.

```text
You are working on the Verity repo native EVMYulLean transition.

Repo:
  $VERITY_REPO

Memory:
  $VERITY_MEMORY_DIR

Before doing anything:
  1. cd "$VERITY_REPO"
  2. source "$HOME/.elan/env" 2>/dev/null || export PATH="$HOME/.elan/bin:$PATH"
  3. Read:
       "$VERITY_MEMORY_DIR"/MEMORY.md
       any native EVMYulLean transition memory files
       any IRStorageWord / IRStorageSlot memory files
       docs/NATIVE_EVMYULLEAN_DONE_GRAPH.md
       docs/NATIVE_EVMYULLEAN_FULL_TRANSITION_DONE.md
  4. git fetch --all --prune
  5. Inspect current open PRs and relevant branches with gh.

Goal:
  Keep native EVMYulLean as the authoritative public compiler-verification
  target.

Current architecture:
  The public proof path runs through:

    Native.interpretRuntimeNative
    Native.interpretIRRuntimeNative
    EvmYul.Yul.callDispatcher

  The removed backend-fuel path must not be reintroduced as a theorem-facing
  authority.

Definition of done:
  Follow docs/NATIVE_EVMYULLEAN_FULL_TRANSITION_DONE.md exactly. Do not claim
  the transition is complete until every criterion in that document holds.

Per-session rule:
  Pick the bottom-most open PR in the native EVMYulLean transition stack that is
  not merge-ready and make it merge-ready. If no PR exists, create the smallest
  useful PR that advances one durable boundary from the "Incremental PR Shape"
  section of docs/NATIVE_EVMYULLEAN_FULL_TRANSITION_DONE.md.

Preferred work order:
  1. Generated-fragment characterization and fail-closed checks.
  2. Native state/environment bridge theorem families.
  3. Native result projection theorem families.
  4. Native statement/expression equivalence by generated construct family.
  5. Dispatcher/native-switch preservation and temporary freshness.
  6. Fuel sufficiency or fuel-parametric preservation.
  7. Generic generated-runtime native agreement.
  8. Public Layer 3 and EndToEnd theorem target flip.
  9. CI, docs, trust-boundary update.
  10. Keep removed interpreter/adapter surfaces out of the public proof path.

Hard constraints:
  - Do not add public theorem assumptions merely to make proofs pass.
  - Do not use sorry, admit, axioms, unchecked declarations, or hidden local
    assumptions.
  - Do not route the final public proof through an alternate Yul interpreter.
  - Do not weaken generated-fragment predicates to include constructs that the
    native proof does not actually cover.
  - Do not paper over environment, storage, memory, returndata, logs, gas, or
    external-call gaps with default values unless a theorem-facing invariant or
    fail-closed validator justifies the default.
  - Do not change CI workflows, branch protection, audit/trust docs, or public
    theorem hypotheses without a precise reason in the PR.
  - Do not hand-edit generated artifacts.
  - Do not use plain git push --force; use git push --force-with-lease.

Validation for proof PRs:
  Run the smallest relevant Lean target first, then the broader stack:

    lake build Compiler.Proofs.YulGeneration.Backends.EvmYulLeanNativeHarness
    lake build Compiler.Proofs.YulGeneration.Backends.EvmYulLeanNativeLowering
    lake build Compiler.Proofs.EndToEnd
    lake build Compiler
    lake build Contracts
    lake build PrintAxioms
    python3 scripts/generate_print_axioms.py --check
    python3 scripts/check_native_transition_doc.py
    python3 scripts/check_bridge_coverage_sync.py
    python3 scripts/check_evmyullean_capability_boundary.py
    make check

Validation for docs-only PRs:
  Run:

    python3 scripts/check_native_transition_doc.py
    python3 scripts/check_docs_workflow_sync.py
    make check

Merge-readiness:
  A PR is ready only when local validation passes, required-equivalent GitHub
  checks are green on the head commit, and all Bugbot/Codex/maintainer review
  threads are resolved. Merge with gh pr merge --squash --delete-branch only
  after those conditions hold.

Memory update rule:
  At the end of the session, update memory with stable findings only:
    - merged PRs and commit SHAs
    - durable proof patterns
    - accepted scope constraints
    - real blockers
    - final architecture decisions

Work style:
  Keep working until the selected PR is merge-ready or a real blocker is
  precisely documented. If a proof is structurally impossible under the current
  theorem surface, stop adding code, write a precise memory note, and explain
  the obstruction.
```

## Completion Checklist

Before declaring the transition complete, verify these exact facts:

```bash
! rg "nativeIRRuntimeAgreesWithEvmYulLeanFuelWrapper|nativeCallDispatcherAgreesWithEvmYulLeanFuelWrapper" Compiler/Proofs/EndToEnd.lean
! rg "\\bsorry\\b|\\badmit\\b|^axiom " Compiler Verity Contracts
python3 scripts/check_axioms.py
python3 scripts/generate_print_axioms.py --check
lake build Compiler.Proofs.EndToEnd
lake build Compiler
lake build Contracts
lake build PrintAxioms
make check
```

The first two `rg` commands should either find no public-theorem dependency on
the old backend-fuel path, or should find only documentation/comments and
historical lemmas explicitly excluded from the public semantic target.
